// Cliente para conexión con LLM (Gemini, OpenAI, etc.)
export const iaClient = {
  analizar: async (prompt) => {
    // Implementar llamada real a la IA
    return 'Resultado IA (simulado)';
  }
};
